fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.9

# GPU PyTorch (CUDA 12.1 - matches RunPod; comment CPU below if needed)
torch==2.4.1+cu121 --index-url https://download.pytorch.org/whl/cu121
torchaudio==2.4.1+cu121 --index-url https://download.pytorch.org/whl/cu121

# CPU fallback (comment out for GPU-only)
# torch==2.4.1 --index-url https://download.pytorch.org/whl/cpu
# torchaudio==2.4.1 --index-url https://download.pytorch.org/whl/cpu

# Basic TTS dependencies (add your preferred TTS library)
librosa==0.10.2
soundfile==0.12.1
numpy==2.1.1

# Uncomment one of these based on your TTS choice:
# tortoise-tts  # High quality but slower
# TTS          # Coqui TTS - good balance
# pyttsx3      # Simple offline TTS

# Required: For IndexTTS-2 model downloads
huggingface-hub==0.25.2   # For model downloads from Hugging Face
transformers==4.45.2      # For HuggingFace models
accelerate==1.1.1         # For faster inference